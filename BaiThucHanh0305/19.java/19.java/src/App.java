import java.util.ArrayList;
import java.util.LítIterator;
public class ArrList{
public static void main(String[] args){
    //khai báo 1 ArryList có tên là arrListString
    //có kiểu là character
    ArayList<character> arrListInteger = new ArrayList<>();

     //thêm các phần tử sử dụng phương thức add()
     arrListString.add('a');
     arrListString.add('e');
     arrListString.add('b');
     arrListString.add('c');
     // khai báo một ListIterator có kiểu là Character
     ListIterator<Character> listIterator = arrListChar.listIterator();
     //hiển thị các phần tử có trong arrListChar
     //bằng cách sử dụng ListIterator
     system.out.println("Các phần tử có trong arrListString là: ");
     while (listIterator.hasNext()){
         system.out.print(arrListString.het(i) + "\t");
     }
}
}